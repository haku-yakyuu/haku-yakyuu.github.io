---
const { title, siteTitle } = Astro.props;
---

<!DOCTYPE html>
<html lang="zh-TW">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{title} | {siteTitle || 'HAKU YAKYUU'}</title>
    <!-- 引入 Montserrat 與 Noto Sans TC -->
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@900&family=Noto+Sans+TC:wght@400;700&family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,0,0&display=swap" rel="stylesheet">
</head>
<body>

    <!-- 1. 頂部導覽列 (Sticky Navbar) -->
    <nav>
        <div class="nav-logo">HAKU</div>
        <ul class="nav-links">
            <li><a href="/">首頁</a></li>
            <li><a href="/category/紀念球">紀念球系列</a></li>
            <li><a href="/category/職人配件">職人配件</a></li>
            <li><a href="/category/服務項目">代購與鑑定</a></li>
        </ul>
        <div class="nav-icons">
            <span class="material-symbols-outlined">search</span>
            <span class="material-symbols-outlined">shopping_bag</span>
            <span class="material-symbols-outlined menu-icon">menu</span>
        </div>
    </nav>

    <!-- 2. 主要內容區 -->
    <main>
        <slot />
    </main>

    <!-- 3. 頁尾 (Footer) -->
    <footer>
        <div class="container footer-content">
            <div>
                <div class="footer-logo">HAKU</div>
                <p style="opacity: 0.6; font-size: 0.9rem;">紀錄每一投一打的經典瞬間。<br>來自職人眼光的棒球紀念品選物店。</p>
            </div>
            <div class="footer-links">
                <h4>SHOP</h4>
                <ul>
                    <li><a href="/">所有商品</a></li>
                    <li><a href="/category/紀念球">紀念球</a></li>
                    <li><a href="/category/職人配件">配件</a></li>
                </ul>
            </div>
            <div class="footer-links">
                <h4>INFO</h4>
                <ul><li>關於我們</li><li>配送政策</li><li>退換貨說明</li></ul>
            </div>
            <div class="footer-links">
                <h4>CONNECT</h4>
                <ul><li>Instagram</li><li>Facebook</li><li>LINE 客服</li></ul>
            </div>
        </div>
        <div class="copyright">© 2025 HAKU YAKYUU SELECT. ALL RIGHTS RESERVED.</div>
    </footer>

</body>
</html>

<style is:global>
    /* --- HAKU V5 Design System Variables --- */
    :root {
        --haku-ink: #3E2723;
        --haku-paper: #F5F1E8;
        --placeholder-bg: #DCD7CE;
        --placeholder-icon: rgba(62, 39, 35, 0.3);
        --section-gap: 120px;
    }

    /* --- Reset & Base --- */
    * { box-sizing: border-box; }
    body { 
        margin: 0; padding: 0; 
        background-color: var(--haku-paper); 
        font-family: 'Noto Sans TC', sans-serif; 
        color: var(--haku-ink);
        line-height: 1.6;
    }
    a { text-decoration: none; color: inherit; transition: opacity 0.2s; }
    a:hover { opacity: 0.7; }
    ul { list-style: none; padding: 0; margin: 0; }

    /* --- Navbar Styles --- */
    nav {
        position: sticky; top: 0; z-index: 1000;
        background-color: var(--haku-paper);
        border-bottom: 1px solid rgba(62, 39, 35, 0.1);
        display: flex; justify-content: space-between; align-items: center;
        padding: 15px 5%;
    }
    .nav-logo { font-family: 'Montserrat', sans-serif; font-weight: 900; font-size: 1.8rem; letter-spacing: 2px; }
    .nav-links { display: none; gap: 30px; font-weight: 700; font-size: 0.9rem; }
    .nav-icons { display: flex; gap: 20px; cursor: pointer; }
    
    @media (min-width: 1024px) { 
        .nav-links { display: flex; } 
        .menu-icon { display: none; }
    }

    /* --- Common Container --- */
    .container { max-width: 1400px; margin: 0 auto; padding: 0 5%; }

    /* --- Section Headers --- */
    section { margin-top: var(--section-gap); }
    .sec-header { 
        border-bottom: 3px solid var(--haku-ink); 
        margin-bottom: 40px; 
        display: flex; justify-content: space-between; align-items: flex-end; 
        padding-bottom: 10px; 
    }
    .sec-header h2 { font-family: 'Montserrat', sans-serif; font-size: 1.5rem; margin: 0; }
    
    /* --- Footer Styles --- */
    footer {
        background-color: var(--haku-ink); color: var(--haku-paper);
        margin-top: var(--section-gap); padding: 80px 5% 40px;
    }
    .footer-content { display: grid; grid-template-columns: 1fr; gap: 40px; }
    @media (min-width: 1024px) { .footer-content { grid-template-columns: 2fr 1fr 1fr 1fr; } }
    
    .footer-logo { font-family: 'Montserrat', sans-serif; font-weight: 900; font-size: 2rem; margin-bottom: 20px; }
    .footer-links h4 { margin-bottom: 20px; border-bottom: 1px solid rgba(245, 241, 232, 0.3); padding-bottom: 10px; font-family: 'Montserrat'; }
    .footer-links li { margin-bottom: 10px; font-size: 0.9rem; opacity: 0.7; }
    .copyright { margin-top: 60px; text-align: center; font-size: 0.8rem; opacity: 0.4; }

    /* --- Grid System Definitions (Global) --- */
    .grid-v { display: grid; gap: 20px; grid-template-columns: repeat(2, 1fr); }
    @media (min-width: 1024px) { .grid-v { grid-template-columns: repeat(4, 1fr); gap: 30px; } }

    .grid-h-img { display: grid; gap: 20px; grid-template-columns: 1fr; }
    @media (min-width: 1024px) { .grid-h-img { grid-template-columns: repeat(3, 1fr); gap: 30px; } }

    .grid-h-noimg { display: grid; gap: 20px; grid-template-columns: 1fr; }
    @media (min-width: 1024px) { .grid-h-noimg { grid-template-columns: repeat(4, 1fr); gap: 20px; } }
</style>
